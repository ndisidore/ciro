pipeline "hello" {
    step "greet" {
        image "alpine:latest"
        run "echo 'Hello from Cicada!'"
    }

    step "info" {
        image "alpine:latest"
        depends-on "greet"
        run "uname -a"
        run "date"
    }
}
